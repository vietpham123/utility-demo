apiVersion: apps/v1
kind: Deployment
metadata:
  name: outage-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: outage-service
  template:
    metadata:
      labels:
        app: outage-service
    spec:
      containers:
      - name: outage-service
        image: outage-service:latest
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: outage-service
spec:
  selector:
    app: outage-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
