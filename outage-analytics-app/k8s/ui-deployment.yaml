apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-ui
  namespace: utility-outage-analytics
  labels:
    app: analytics-ui
spec:
  replicas: 2
  selector:
    matchLabels:
      app: analytics-ui
  template:
    metadata:
      labels:
        app: analytics-ui
    spec:
      containers:
      - name: analytics-ui
        image: localhost:5000/analytics-ui:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 3
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-ui
  namespace: utility-outage-analytics
spec:
  type: LoadBalancer
  selector:
    app: analytics-ui
  ports:
  - port: 80
    targetPort: 80
